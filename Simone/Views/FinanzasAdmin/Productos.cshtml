@model List<Simone.Services.ProductoVentaResumen>
@{
    ViewData["Title"] = "Productos más vendidos";
    var periodo  = ViewBag.Periodo  as string ?? "mes";
    var desde    = ViewBag.Desde    as string ?? "";
    var hasta    = ViewBag.Hasta    as string ?? "";
    var busqueda = ViewBag.Busqueda as string ?? "";
    var top      = (int)(ViewBag.Top ?? 20);
    var maxUnidades = Model.Any() ? Model.Max(p => p.UnidadesVendidas) : 1;
    if (maxUnidades == 0) maxUnidades = 1;
}

@section Styles {
<style>
@@import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&family=Syne:wght@700;800&display=swap');
:root{--ink:#0d0f14;--ink-2:#3d4150;--ink-3:#8b90a0;--paper:#f4f3f0;--paper-2:#eceae5;--paper-3:#e2dfd8;--line:#d8d5ce;--accent:#c9472b;--accent-lt:#fdf1ee;--green:#1a7a4a;--green-lt:#edfaf3;--amber:#b45309;--amber-lt:#fef9ec;--r:12px;--r-lg:18px;--sh:0 1px 3px rgba(0,0,0,.06);--ease:cubic-bezier(.4,0,.2,1);}
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0;}
body{font-family:'Space Grotesk',sans-serif;background:var(--paper);color:var(--ink);font-size:14px;}
.fa-wrap{max-width:1400px;margin:0 auto;padding:2rem 1.5rem 4rem;}
.fa-topbar{display:flex;align-items:flex-start;justify-content:space-between;gap:1.5rem;flex-wrap:wrap;margin-bottom:1.75rem;padding-bottom:1.5rem;border-bottom:2px solid var(--ink);}
.fa-heading-label{font-size:.68rem;font-weight:600;letter-spacing:.12em;text-transform:uppercase;color:var(--accent);display:block;margin-bottom:.25rem;}
.fa-heading h1{font-family:'Syne',sans-serif;font-size:2rem;font-weight:800;letter-spacing:-.03em;line-height:1;}
.fa-nav{display:flex;gap:.3rem;margin-bottom:1.5rem;flex-wrap:wrap;}
.fa-nav-item{display:flex;align-items:center;gap:.4rem;padding:.5rem .9rem;border-radius:var(--r);font-size:.78rem;font-weight:600;color:var(--ink-2);text-decoration:none;transition:all .15s;}
.fa-nav-item:hover{background:var(--paper-2);color:var(--ink);}
.fa-nav-item.active{background:var(--ink);color:var(--paper);}
.filtros-bar{display:flex;align-items:center;gap:.65rem;flex-wrap:wrap;margin-bottom:1.25rem;padding:1rem 1.2rem;background:var(--paper);border:1.5px solid var(--line);border-radius:var(--r-lg);box-shadow:var(--sh);}
.filtros-bar input{padding:.5rem .8rem;border:1.5px solid var(--line);border-radius:var(--r);font-family:'Space Grotesk',sans-serif;font-size:.8rem;color:var(--ink);background:var(--paper);height:36px;min-width:200px;}
.filtros-bar input:focus{outline:none;border-color:var(--ink);}
.btn-filtrar{padding:.5rem 1.1rem;background:var(--ink);color:var(--paper);border:none;border-radius:var(--r);font-family:'Space Grotesk',sans-serif;font-size:.8rem;font-weight:600;cursor:pointer;height:36px;display:flex;align-items:center;gap:.4rem;}
.btn-filtrar:hover{opacity:.85;}

.tabla-wrap{background:var(--paper);border:1.5px solid var(--line);border-radius:var(--r-lg);overflow:hidden;box-shadow:var(--sh);}
.fa-table{width:100%;border-collapse:collapse;}
.fa-table th{font-size:.67rem;font-weight:700;letter-spacing:.07em;text-transform:uppercase;color:var(--ink-3);padding:.75rem 1rem;text-align:left;border-bottom:2px solid var(--line);background:var(--paper-2);white-space:nowrap;}
.fa-table td{padding:.8rem 1rem;font-size:.82rem;color:var(--ink-2);border-bottom:1px solid var(--paper-2);vertical-align:middle;}
.fa-table tr:last-child td{border-bottom:none;}
.fa-table tr:hover td{background:var(--paper-2);}

.td-rank{font-family:'Syne',sans-serif;font-size:1.1rem;font-weight:800;color:var(--paper-3);text-align:center;}
.td-rank.top1{color:#d4af37;}.td-rank.top2{color:#9ca3af;}.td-rank.top3{color:#cd7f32;}
.td-num{font-weight:700;font-variant-numeric:tabular-nums;color:var(--ink);}
.td-green{color:var(--green);font-weight:700;}
.td-sub{font-size:.72rem;color:var(--ink-3);display:block;margin-top:1px;}

.prod-thumb{width:42px;height:42px;border-radius:8px;object-fit:cover;border:1px solid var(--line);background:var(--paper-2);}
.prod-info{display:flex;align-items:center;gap:.75rem;}
.prod-name{font-weight:600;color:var(--ink);}

.bar-inline{display:flex;align-items:center;gap:.5rem;}
.bar-bg{width:100px;height:6px;background:var(--paper-3);border-radius:3px;overflow:hidden;}
.bar-fill{height:100%;background:var(--accent);border-radius:3px;transition:width .6s var(--ease);}

.badge{display:inline-flex;align-items:center;padding:.2rem .55rem;border-radius:50px;font-size:.68rem;font-weight:700;}
.badge-gray{background:var(--paper-3);color:var(--ink-3);}
.badge-amber{background:var(--amber-lt);color:var(--amber);}

.top-selector{display:flex;gap:.3rem;align-items:center;}
.top-btn{padding:.4rem .75rem;border:1.5px solid var(--line);border-radius:var(--r);font-size:.75rem;font-weight:600;color:var(--ink-3);text-decoration:none;transition:all .15s;}
.top-btn.active,.top-btn:hover{border-color:var(--ink);color:var(--ink);background:var(--paper-2);}
.top-btn.active{background:var(--ink);color:var(--paper);}

.empty-state{text-align:center;padding:4rem 2rem;color:var(--ink-3);}
</style>
}

<div class="fa-wrap">
    <div class="fa-topbar">
        <div class="fa-heading">
            <span class="fa-heading-label">Centro Financiero</span>
            <h1>Productos más vendidos</h1>
        </div>
        <div class="top-selector">
            <span style="font-size:.75rem;font-weight:600;color:var(--ink-3)">Mostrar top:</span>
            @foreach (var t in new[] { 10, 20, 50 })
            {
                <a href="/FinanzasAdmin/Productos?periodo=@periodo&top=@t&busqueda=@busqueda"
                   class="top-btn @(top == t ? "active" : "")">@t</a>
            }
        </div>
    </div>

    <nav class="fa-nav">
        <a href="/FinanzasAdmin"           class="fa-nav-item">Dashboard</a>
        <a href="/FinanzasAdmin/Ingresos"  class="fa-nav-item">Ingresos</a>
        <a href="/FinanzasAdmin/Ventas"    class="fa-nav-item">Ventas</a>
        <a href="/FinanzasAdmin/Vendedores" class="fa-nav-item">Vendedores</a>
        <a href="/FinanzasAdmin/Productos" class="fa-nav-item active">Productos</a>
        <a href="/FinanzasAdmin/Clientes"  class="fa-nav-item">Clientes</a>
    </nav>

    <form method="get" action="/FinanzasAdmin/Productos">
        <input type="hidden" name="periodo" value="@periodo" />
        <input type="hidden" name="top" value="@top" />
        <div class="filtros-bar">
            <input type="text" name="busqueda" value="@busqueda" placeholder="Buscar producto o categoría..." />
            <label style="font-size:.75rem;font-weight:600;color:var(--ink-3)">Desde</label>
            <input type="date" name="desde" value="@desde" style="min-width:unset" />
            <label style="font-size:.75rem;font-weight:600;color:var(--ink-3)">Hasta</label>
            <input type="date" name="hasta" value="@hasta" style="min-width:unset" />
            <button type="submit" class="btn-filtrar">
                <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
                Filtrar
            </button>
            @foreach (var p in new[] { ("mes","Mes"), ("trimestre","Trimestre"), ("anio","Año") })
            {
                <a href="/FinanzasAdmin/Productos?periodo=@p.Item1&top=@top"
                   style="padding:.45rem .8rem;border:1.5px solid @(periodo==p.Item1?"var(--ink)":"var(--line)");border-radius:var(--r);font-size:.75rem;font-weight:600;color:@(periodo==p.Item1?"var(--ink)":"var(--ink-3)");text-decoration:none;display:flex;align-items:center;height:36px;">@p.Item2</a>
            }
        </div>
    </form>

    <div class="tabla-wrap">
        @if (!Model.Any())
        {
            <div class="empty-state"><p>Sin datos para el período seleccionado.</p></div>
        }
        else
        {
            <div style="overflow-x:auto">
                <table class="fa-table">
                    <thead>
                        <tr>
                            <th style="width:50px">#</th>
                            <th>Producto</th>
                            <th>Categoría</th>
                            <th>Vendedor</th>
                            <th>Unidades</th>
                            <th>Ingreso Total</th>
                            <th>Precio Promedio</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var (p, i) in Model.Select((p, i) => (p, i)))
                        {
                            var rank = i + 1;
                            var rankClass = rank == 1 ? "top1" : rank == 2 ? "top2" : rank == 3 ? "top3" : "";
                            var pct = (int)((double)p.UnidadesVendidas / maxUnidades * 100);
                            <tr>
                                <td><span class="td-rank @rankClass">@rank</span></td>
                                <td>
                                    <div class="prod-info">
                                        @if (!string.IsNullOrEmpty(p.ImagenPath))
                                        {
                                            <img src="@p.ImagenPath" class="prod-thumb" alt="@p.Nombre"
                                                 onerror="this.style.display='none'" />
                                        }
                                        <span class="prod-name">@p.Nombre</span>
                                    </div>
                                </td>
                                <td><span class="badge badge-gray">@p.Categoria</span></td>
                                <td>
                                    <span style="font-size:.8rem;font-weight:500">@p.NombreVendedor</span>
                                </td>
                                <td>
                                    <div class="bar-inline">
                                        <span class="td-num">@p.UnidadesVendidas</span>
                                        <div class="bar-bg">
                                            <div class="bar-fill" style="width:@pct%"></div>
                                        </div>
                                    </div>
                                </td>
                                <td><span class="td-green">$@p.IngresoTotal.ToString("N2")</span></td>
                                <td><span class="td-num">$@p.PrecioPromedio.ToString("N2")</span></td>
                            </tr>
                        }
                    </tbody>
                    <tfoot>
                        <tr style="background:var(--paper-2)">
                            <td colspan="4" style="font-weight:700;font-size:.8rem;padding:.85rem 1rem">TOTALES</td>
                            <td><span class="td-num">@Model.Sum(p => p.UnidadesVendidas)</span></td>
                            <td><span class="td-green">$@Model.Sum(p => p.IngresoTotal).ToString("N2")</span></td>
                            <td></td>
                        </tr>
                    </tfoot>
                </table>
            </div>
        }
    </div>
</div>
